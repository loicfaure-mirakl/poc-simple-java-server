services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: simple_javalin_app
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5455:5432"
